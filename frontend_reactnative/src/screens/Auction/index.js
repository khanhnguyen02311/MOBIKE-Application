import React from 'react';
import {Text, View} from 'react-native';
import AuctionComponent from '../../components/Auction';

const Auction = ({params}) => <AuctionComponent />;

export default Auction;
