import React from 'react';
import { Text, View } from 'react-native';

const PendingRoute = ({
    params,
}) => {

    return (
        <View style={{}}>
            <Text>PendingRoute</Text>
        </View>
    )
};

export default PendingRoute;
