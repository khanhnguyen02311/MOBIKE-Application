import React from 'react';
import {Text, View} from 'react-native';

const SavedPostComponent = ({params}) => (
  <View>
    <Text>SavedPostComponent</Text>
  </View>
);

export default SavedPostComponent;
