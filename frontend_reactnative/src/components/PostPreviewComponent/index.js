import React from 'react';
import { Text, View } from 'react-native';

const PostPreviewComponent = ({
    params,
}) => (
    <View>
        <Text>PostPreviewComponent</Text>
    </View>
);

export default PostPreviewComponent;
